# Bad PostgreSQL parameters file for testing only.
# MISSING db_properties section: should cause an error.
#
[DEFAULT]
  DB_MAX_ACTIVE = 10
  DEFAULT_TAP_USER = 'tapuser'
  VALIDATION_QUERY = SELECT 1
  VALIDATION_QUERY_TIMEOUT = 2
